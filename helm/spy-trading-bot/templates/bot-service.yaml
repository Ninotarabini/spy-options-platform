apiVersion: v1
kind: Service
metadata:
  name: bot-service
  namespace: spy-options-bot
  labels:
    app: trading-bot
spec:
  type: ClusterIP
  clusterIP: None  # Headless
  selector:
    app: trading-bot
