apiVersion: v1
kind: Service
metadata:
  name: detector-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: detector
spec:
  type: ClusterIP
  clusterIP: None  # Headless - para comunicación directa pod-to-pod
  selector:
    app: detector
