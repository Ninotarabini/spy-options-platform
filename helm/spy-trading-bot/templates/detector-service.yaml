apiVersion: v1
kind: Service
metadata:
  name: detector-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: detector
spec:
  type: ClusterIP
  ports:
  - port: 9100
    targetPort: 9100
    protocol: TCP
    name: metrics  # Match ServiceMonitor selector
  selector:
    app: detector
